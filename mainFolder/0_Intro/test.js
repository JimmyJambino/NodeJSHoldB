const age = 2;
console.log("Your age is: ", age) // type coercion, don't concatonate different data types.
// command REPL? jshell, python, ipython etc.
// git add -A  (adds everything, if . instead of -A, then only the current folder)
// git commit -m "Added message"    (commits)
// git push